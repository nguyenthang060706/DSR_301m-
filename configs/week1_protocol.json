{
  "project": "robustness_aware_attention_distillation_waste",
  "seed": 20260913,
  "classes": [
    "cardboard",
    "glass",
    "metal",
    "paper",
    "plastic",
    "trash"
  ],
  "data": {
    "trashnet_root": "data/raw/trashnet",
    "split_dir": "data/splits",
    "dev_corruption_holdout_ratio": 0.15,
    "cv_folds": 5,
    "image_size": 224,
    "num_workers": 4
  },
  "training_protocol_status": "locked",
  "internal_ablation_protocol": {
    "optimizer": "sgd",
    "momentum": 0.9,
    "weight_decay": 0.0001,
    "batch_size": 32,
    "max_epochs": 100,
    "loss_function": "weighted_cross_entropy_inverse_frequency",
    "early_stopping": {
      "enabled": false,
      "reason": "Protocol lock requires identical epoch budgets across internal ablations."
    },
    "lr_schedule": {
      "type": "cosine",
      "base_lr": 0.01,
      "min_lr": 0.000001,
      "warmup_epochs": 5
    }
  },
  "kd_defaults_to_confirm_later": {
    "alpha_logit": 0.7,
    "temperature_candidates": [2, 3, 6],
    "gamma_attention_candidates": [0.1, 0.5, 1.0],
    "delta_consistency_candidates": [0.1, 0.5, 1.0]
  },
  "leakage_rules": {
    "dev_corruption_holdout_never_in_cv_train": true,
    "delta_tuning_never_uses_severity_3": true,
    "trashnet_c_source": "dev_corruption_holdout_only"
  }
}
